let i = 0
while (i <= 10) {
    // console.log(`Value of index is ${i}`);
    i++
}

let myarr = ['flash', 'batman', 'super']
let val = 0

while (val < myarr.length) {
    // console.log(`Value is ${myarr[val]}`);
    val++

}


// do while loop performs the task atleast once
let score = 0
do {
    console.log(`Score ${score}`);
    score++

} while (score <= 10);